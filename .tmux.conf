unbind r
bind r source-file ~/.tmux.conf \; display-message "Config reloaded..."

# Unbind tab tmux mapping - it's annoying
unbind -n Tab


set -g prefix C-s

# Pane navigation
setw -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# Binds to place nice with Claude Code 
bind-key -T root S-Enter send-keys Escape "[13;2u" # i just wanna be able to press shift+enter, man



# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'dracula/tmux'

set -g @dracula-show-powerline true
set -g @dracula-fixed-location "San Diego"
set -g @dracula-plugins "weather"
set -g @dracula-show-flags true
set -g @dracula-show-left-icon session
set -g status-position top

# Initialize TMUX plugin manager
run '~/.tmux/plugins/tpm/tpm'
